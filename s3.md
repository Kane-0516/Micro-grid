# S3 Upload Status

Last checked: 2026-04-08 17:00 CST (Asia/Shanghai)

## Current process

- Running process: `aws.exe`
- PID: `76084`
- Start time: `2026-04-08 15:11:44`
- Status at check time: process still alive, CPU usage still increasing slightly

## Active sync logs

- Main sync log: `deploy/pelias_s3_sync_20260408_151144.out.log`
- Error log: `deploy/pelias_s3_sync_20260408_151144.err.log`

Observed state:

- Main sync log last update: `2026-04-08 16:30:15`
- Main sync log recorded `504` `upload:` lines
- Error log size: `0`
- Last visible uploaded files were near the end of the path ordering, including:
  - `openaddresses/za/...`
  - `placeholder/wof.extract`
  - `placeholder/store.sqlite3`

Interpretation:

- This run appears to have reached a late stage of traversal.
- The process is still alive, but the sync log stopped updating.
- That suggests the upload is no longer progressing normally.

## Aggregate progress log found

- Progress log: `deploy/pelias_s3_upload.out.log`
- Matching error log: `deploy/pelias_s3_upload.err.log`

Last visible aggregate progress line:

```text
Completed 41.6 GiB/52.6 GiB (460.3 KiB/s) with 1 file(s) remaining
```

Approximate progress from that line:

- Uploaded: `41.6 GiB`
- Total: `52.6 GiB`
- Approximate completion: `79.1%`

Important note:

- This percentage is the latest aggregate progress line found in logs.
- It is not guaranteed to be the real-time status of the currently running process.

## Errors observed

From `deploy/pelias_s3_upload.err.log`:

- Count of `upload failed:` lines observed: `1044`
- Repeated failure type:

```text
Could not connect to the endpoint URL
```

Affected examples include uploads under:

- `openaddresses/us/wy/...`
- `openaddresses/uy/...`
- `openaddresses/za/...`
- `placeholder/...`
- `whosonfirst/sqlite/...`
- `openstreetmap/us-latest.osm.pbf`

Interpretation:

- The upload encountered repeated S3 endpoint connectivity failures.
- The current `aws.exe` process may be stuck in retry/wait behavior rather than making healthy forward progress.

## Local file count reference

Under `.tools/pelias-data/us`:

- Total local file count observed: `10008`

Important note:

- This cannot be used as an exact upload percentage denominator.
- `aws s3 sync` may skip unchanged files or files already present remotely.

## Current conclusion

- The upload did run and reached a late stage.
- A log indicates it reached at least `41.6/52.6 GiB` with `1` file remaining at one point.
- The currently running process does not appear to be uploading normally now.
- The most likely issue is repeated network or endpoint connectivity failure to S3.

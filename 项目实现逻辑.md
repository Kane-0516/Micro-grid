# MicroGrid 项目实现逻辑

本文基于当前仓库代码梳理项目的真实实现方式，目标是把以下问题讲清楚：

- 整个项目前后端是怎么协作的
- 页面上的业务流程是如何驱动接口调用的
- 请求、响应、前端状态、后端计算结果分别怎么流动
- Pelias 地图与地理编码功能到底是怎么接进来的
- 这个项目哪些数据是“持久化”的，哪些只是“运行时传递”

结论先说：

- 这是一个 `React + Vite + TypeScript` 前端，配合 `FastAPI` 后端的单页应用。
- 前端没有使用前端路由库来切页面，而是用 `App.tsx` 里的状态机切换欢迎页、标准产品页、向导页、方案选择页、结果页。
- 后端没有数据库，核心是“接收配置 -> 计算/仿真 -> 返回结果”。
- 地图显示用的是 `Leaflet + ArcGIS World Imagery` 瓦片。
- 地址搜索与逆地理编码不是前端直连 Pelias，而是 `前端 -> 本项目后端代理接口 -> Pelias`。
- Pelias 本身是一个独立基础服务，通常通过 Docker 单独启动；它不是这个前后端仓库内部的一部分业务逻辑。

---

## 1. 项目总体架构

### 1.1 目录与分层

项目主要分成四块：

- `frontend/`
  - React 前端页面、表单、地图、结果展示、报告下载弹窗
- `backend/`
  - FastAPI 接口层、参数校验、选型计算、PyPSA 仿真、经济分析、报表生成
- `pelias/`
  - Pelias 本地部署与导数脚本、区域配置
- `deploy/`
  - Docker Compose 部署文件，尤其是 Pelias 相关容器编排

### 1.2 运行时真实拓扑

开发模式下，实际运行关系是：

```text
浏览器
  -> Vite 前端开发服务器 :5173
     -> /api/* 被 Vite 代理到 FastAPI :6001
        -> 计算类接口在 FastAPI 内部完成
        -> 地理编码接口再由 FastAPI 转发到 Pelias :4000

Pelias
  -> 独立运行在 :4000
  -> 提供 /v1/search 与 /v1/reverse
```

### 1.3 为什么说它是“无数据库计算型系统”

从代码看：

- 前端配置保存在 React `useState` 和 Zustand store 中
- 后端没有 SQLAlchemy、MySQL、PostgreSQL、MongoDB 之类的数据层
- 产品目录来自 `backend/products.yaml`
- 计算结果不落库，直接通过接口返回
- 报告导出也是即时生成 Excel 后返回给前端

所以这个系统本质上是：

```text
静态产品参数 + 用户输入 + 运行时仿真/计算 = 页面结果 / 导出报表
```

不是传统“增删改查型”业务系统。

---

## 2. 系统启动与服务接线方式

### 2.1 `start-all.bat` 做了什么

`start-all.bat` 是整个开发链路的总入口，它做了 4 件核心事情：

1. 写入前端环境文件 `frontend/.env.local`
2. 检查 `http://localhost:4000` 上的 Pelias 是否可用
3. 启动后端 FastAPI，并把 Pelias 相关环境变量注入后端
4. 启动前端 Vite

也就是说，Pelias 不是这个脚本启动的，而是它的前置条件。

### 2.2 前端怎么连后端

`frontend/vite.config.ts` 配置了：

```text
/api -> http://127.0.0.1:6001
```

所以浏览器里所有 `fetch('/api/...')`：

- 看起来像打到前端域名
- 实际由 Vite 代理到 FastAPI

这样做有两个直接效果：

- 前端开发时不需要写死跨域 URL
- 浏览器不会因为开发时的跨域问题频繁报错

### 2.3 后端怎么连 Pelias

`start-all.bat` 启动后端时注入这些环境变量：

- `GEOCODER_API_URL=http://localhost:4000/v1/search`
- `GEOCODER_REVERSE_API_URL=http://localhost:4000/v1/reverse`
- 以及 CN / US 两套 URL

后端 `backend/app/core/config.py` 把这些环境变量读入配置常量；`backend/app/routers/geocode.py` 再用这些常量去调用 Pelias。

所以 Pelias 的真实接入链是：

```text
浏览器
  -> /api/geocode 或 /api/reverse-geocode
  -> FastAPI geocode router
  -> Pelias /v1/search 或 /v1/reverse
  -> FastAPI 归一化结果
  -> 返回前端
```

---

## 3. 前端整体实现方式

### 3.1 前端不是多页面，也不是 React Router

前端主入口是 `frontend/src/main.tsx`：

- 应用挂载前先调用 `useProductsStore.getState().init()`
- 这一步会先尝试从后端 `/api/products` 拉产品目录

真正的页面控制在 `frontend/src/App.tsx`。

`App.tsx` 用几个状态变量来驱动整个前端：

- `showWelcome`
- `navPage`
- `currentStep`
- `config`
- `apiAvailable`

其中最关键的是 `navPage` 和 `currentStep`。

### 3.2 `navPage` 决定当前处于哪个业务区

`navPage` 可能是：

- `standard-small`
- `standard-medium`
- `standard-large`
- `custom-known`
- `custom-diy`

含义是：

- `standard-*`：标准产品展示页
- `custom-known`：已知负载向导
- `custom-diy`：DIY 向导

因此，前端的“页面跳转”本质不是路由跳转，而是状态切换。

### 3.3 `config` 是前端最核心的业务数据容器

`config` 类型定义在 `frontend/src/types/index.ts` 的 `ConfigData`。

它包含了整个方案配置过程中几乎所有用户输入与中间值，例如：

- 场景 `scenario`
- 支架套数 `bracketSets`
- 组件型号 `panelModel`
- 电池型号 `batteryPackModel`
- 柴油机是否存在、功率、新旧状态
- 电压等级
- 储能天数
- 负荷类型、年负荷
- 经纬度、地址名
- 峰值日照小时、年等效日照小时
- 场地面积、排布可安装套数
- DIY 场景中的逆变器数、电池包数等

这个 `config` 会贯穿：

- 向导表单展示
- 地图联动
- 方案推荐请求
- 正式计算请求
- 结果页渲染
- 报告导出

可以把它理解成前端业务的“单一事实源”。

### 3.4 前端产品目录是怎么来的

前端在 `main.tsx` 启动时调用 Zustand store 的 `init()`。

`frontend/src/store/useProductsStore.ts` 中的逻辑是：

1. 先内置一套本地静态产品数据 `frontend/src/data/products.ts`
2. 调用后端 `fetchProducts()` 访问 `/api/products`
3. 如果成功：
   - 把后端 `products.yaml` 返回的数据解析成前端结构
   - 覆盖本地静态数据
4. 如果失败：
   - 继续使用前端本地静态数据

这意味着产品目录设计成了“双保险”：

- 正常情况下以前后端统一产品目录为准
- 后端不可用时前端仍然能维持基本交互

---

## 4. 前端业务状态机是怎么运转的

### 4.1 启动后页面切换规则

`App.tsx` 的大致决策顺序如下：

1. `showWelcome=true`
   - 显示欢迎页
2. 如果进入方案选择态 `showPlanSelection=true`
   - 显示候选方案页
3. 如果处于向导流程且 `currentStep === totalWizardSteps + 1`
   - 显示结果页
4. 如果不是向导页
   - 显示标准产品页
5. 否则
   - 显示向导页 `WizardFlowPage`

所以，结果页不是独立路由，而是“向导最后一步之后的一个特殊状态”。

### 4.2 向导页是怎么拼起来的

`WizardFlowPage` 只做布局编排：

- 左侧 `WizardLeftPanel`
- 右侧 `QuestionCard + WizardStepContent`

右侧负责让用户填数据，左侧负责：

- 在某些步骤显示地图 `SiteAreaMap`
- 在其他步骤显示配置拓扑图 `ConfigTopology`
- 以及显示配置摘要

也就是说，页面不是先算完再显示地图，而是地图从向导中途就已经直接参与了配置输入。

### 4.3 步骤控制逻辑在哪里

`App.tsx` 调用了这一批工具函数：

- `getTotalWizardSteps`
- `getStepType`
- `getStepTitle`
- `getStepDescription`
- `canProceedStep`
- `getStepProceedBlockers`
- `shouldShowSiteAreaMap`

这些函数共同决定：

- 当前第几步显示什么表单
- 是否允许下一步
- 是否要切到地图模式
- 下一步按钮上的文案是什么

所以前端向导本质是“规则驱动型状态机”。

---

## 5. 已知负载流程：前后端交互与数据流

这一条是项目最完整的核心业务链。

### 5.1 前端输入阶段

用户在 `custom-known` 流程里逐步填写：

- 位置
- 可用场地
- 支架参数
- 柴油机信息
- 电压等级
- 年负荷
- 储能需求
- EMS 模式

这些数据不断通过 `updateConfig()` 合并进 `config`。

### 5.2 位置输入如何进入 `config`

位置相关表单主要在 `StepLocation.tsx`。

它支持三种定位方式：

1. 文本搜索地址
2. 获取浏览器当前位置
3. 在地图上点选或拖动标记

无论哪种方式，最后都会落到：

```text
config.latitude
config.longitude
config.locationName
```

并且在坐标变化后自动触发太阳辐照资源查询，把结果写入：

- `peakSunHoursPerDay`
- `annualEffHours`
- `annualKwhPerM2`

### 5.3 场地测量如何进入 `config`

左侧地图组件 `SiteAreaMap.tsx` 在多边形测量完成后，会通过 `onAreaMeasured` 回传：

- `grossAreaM2`
- `usableAreaM2`
- `installableSets`
- `perimeterM`
- `vertexCount`

`WizardLeftPanel.tsx` 把这些值写回 `config`：

- `availableAreaM2 = usableAreaM2`
- `grossAreaM2 = grossAreaM2`
- `maxBracketSetsByLayout = installableSets`

同时如果当前用户手动填的 `bracketSets` 大于地图排布上限，还会被截断到可安装上限。

这一步非常关键，因为后面的方案推荐会读取这个面积约束。

### 5.4 点击“生成方案”后发生什么

`App.tsx` 到最后一步时，调用 `useWizardCalculation().runCalculation()`。

如果当前场景是 `known-load`，并且有有效 `annualLoadKwh`，流程不是直接算最终结果，而是先走推荐链路：

```text
前端 runCalculation
  -> /api/optimize
  -> 后端返回候选方案列表
  -> 前端进入 showPlanSelection=true
  -> 用户选中某个方案
  -> 再调用 /api/calculate?simulate=true
  -> 结果页展示正式结果
```

这意味着“推荐”和“正式结果”是两阶段。

### 5.5 `/api/optimize` 请求体是怎么构造的

前端 `useWizardCalculation.ts` 组装了这些关键字段：

- `annualLoadKwh`
- `peakSunHours`
- `dieselPriceUsdPerLiter`
- `dieselIsNew`
- `panelModel`
- `bracketModel`
- `batteryPackModel`
- `availableAreaM2`
- `existingDieselKw`
- `maxBracketSets`
- `objective='payback'`
- `allowDiesel`

其中 `maxBracketSets` 的推导逻辑很重要：

1. 如果地图已经算出 `maxBracketSetsByLayout`
   - 直接用这个值
2. 否则如果已有 `availableAreaM2`
   - 用 `availableAreaM2 / 单套支架面积` 向下取整
3. 再不行就默认 8 套

也就是说，地图测量的结果会直接影响推荐方案扫描范围。

### 5.6 后端 `/api/optimize` 做了什么

后端入口在 `backend/app/routers/optimize.py`。

它的职责很简单：

1. 用 Pydantic `OptimizeRequest` 校验参数
2. 转换成内部 `OptimizeInput`
3. 调用 `app.services.optimizer.optimize()`
4. 把结果包装成响应 JSON

真正的算法核心在 `backend/app/services/optimizer.py`。

### 5.7 `optimizer.py` 的推荐逻辑

推荐链路并不跑完整 PyPSA，而是快估模型。

主要逻辑是：

1. 读取产品目录 `catalog`
2. 根据年负荷推断一个对比用柴油机功率
3. 计算面积允许的最大支架套数
4. 遍历 `min_bracket_sets -> effective_max_sets`
5. 对每个套数：
   - 算出 `pv_kw`
   - 算出目标储能 `battery_kwh`
   - 算出电池包数 `num_packs`
   - 调用 `quick_estimate()` 做快速仿真估算
   - 调用 `build_template_cost_breakdown()` 做 CAPEX/OPEX 拆分
   - 调用 `generate_homer_economic_report()` 做经济性测算
   - 根据 `payback / npv / solar fraction / lcoe` 计算综合评分
6. 排序后返回候选方案

因此 `/api/optimize` 返回的不是单一结果，而是一组候选方案 `options[]`。

### 5.8 候选方案回前端后怎么处理

前端 `useWizardCalculation.ts` 收到 `optRes.options` 后：

- 保存到 `planOptions`
- 保存 `planDieselKw`
- 把 `showPlanSelection` 置为 `true`

随后 `App.tsx` 不再显示向导，而改为显示 `PlanSelectionPage`。

也就是说，用户此时看到的不是最终结果，而是推荐方案列表。

### 5.9 方案选中后如何进入正式计算

用户选中某个推荐方案时，触发 `handlePlanSelect(opt)`。

这一步会把推荐方案中的关键配置重新写回 `config`：

- `bracketSets = opt.bracketSets`
- `dieselCapacityKw = opt.dieselKw`
- `hasGenerator = opt.dieselKw > 0`
- `dieselIsNew = opt.dieselIsNew`
- `storageDays = 1`
- `emsControlMethod = 'edge'`

然后立即调用：

```text
calculateFull(updatedConfig)
-> POST /api/calculate?simulate=true
```

这一步才是正式结果链路。

### 5.10 `/api/calculate?simulate=true` 的后端链路

入口：`backend/app/routers/calculate.py`

内部顺序非常清晰：

1. `get_catalog()`
   - 读取并缓存 `products.yaml`
2. `compute_sizes(req, catalog)`
   - 根据用户输入推导系统规模
3. 根据 `simulate` 决定：
   - `run_pypsa(...)` 正式仿真
   - 或 `quick_estimate(...)` 快速估算
4. `build_template_cost_breakdown(...)`
   - 生成 CAPEX / OPEX 拆分
5. `generate_homer_economic_report(...)`
   - 生成生命周期经济分析
6. 整理为统一 JSON 返回给前端

### 5.11 `compute_sizes()` 在干什么

`backend/app/services/calculator.py` 的 `compute_sizes()` 负责把“表单参数”变成“工程规模参数”。

它会算出：

- `pv_capacity_kw`
- `battery_capacity_kwh`
- `num_battery_packs`
- `diesel_capacity_kw`
- `diesel_kw_comparison`
- `annual_load_kwh`
- `occupied_area_m2`
- `inverter_count`
- `tray_count`

其中关键推导包括：

1. 光伏容量

```text
pv_kw = bracketSets * panels_per_set * panel.kw
```

2. 年负荷

- 如果用户给了 `annualLoadKwh`，直接用
- DIY 模式可根据电流和电压推算
- 其他情况下会按经验公式估算

3. 电池容量

```text
battery_kwh_target = max(
  pv_kw * 3 * storageDays,
  daily_load_kwh * autonomy_factor * storageDays / 0.9
)
```

4. 逆变器和托盘数

- 根据电池包数和逆变器适配包数来推

5. 占地面积

- 调用 `site_rules.py` 里的站点面积规则计算

所以 `compute_sizes()` 是所有后续仿真的“前置规格化”步骤。

### 5.12 正式仿真 `run_pypsa()` 在干什么

`backend/app/services/simulator.py` 的 `run_pypsa()` 负责完整链路：

1. 生成 PV 年度时序
2. 生成负荷年度时序
3. 构造离网微电网仿真器
4. 跑全年 8760 小时 PyPSA 调度
5. 计算柴油油耗
6. 计算纯柴油对照场景
7. 生成光伏-柴油月度分析图数据

它内部调用的关键对象是：

- `generate_pv_profile()`
- `generate_load_profile()`
- `OffGridMicrogridSimulator`
- `DieselOnlySimulator`

### 5.13 PV 时序怎么生成

`backend/app/services/microgrid_simulator.py` 中：

- 优先调用 NASA POWER 小时级气象数据
- 再用 `pvlib` 结合：
  - 太阳位置
  - 辐照分解
  - 组件温度模型
  - PVWatts DC/AC 模型
- 最终生成每个小时的 `pv_cf`，也就是 0 到 1 之间的光伏可用系数

如果 NASA POWER 拉取失败，则退回简化经验光伏曲线。

所以正式仿真中的光伏并不是简单“固定峰值日照小时”，而是小时级时间序列。

### 5.14 负荷时序怎么生成

同一个文件里 `generate_load_profile()` 根据：

- `annual_consumption_kwh`
- `load_type`
- `year`

构造全年 8760 小时的 `load_kw` 序列。

不同负荷类型使用不同日内曲线：

- `residential`
- `commercial`
- `industrial`

再叠加季节性变化，最后归一化到用户给定年负荷。

### 5.15 PyPSA 网络长什么样

`OffGridMicrogridSimulator.build_network()` 创建的网络包括：

- `Bus: AC_bus`
- `Generator: PV`
- `StorageUnit: Battery`
- `Generator: Diesel`
- `Load: Load`
- `Generator: Curtailment`
- `Generator: LoadShedding`

其中：

- `Curtailment` 用负向发电表示弃光吸收
- `LoadShedding` 用高惩罚成本表示负荷缺供

这套建模允许系统在全年每小时求最优调度。

### 5.16 仿真输出了什么

`run_simulation()` 返回核心指标：

- 年光伏发电量
- 年负荷电量
- 年缺供电量
- 年弃光电量
- 年柴油发电量
- 年电池充放电量
- `solar_fraction`
- `loss_of_load_rate`
- `curtailment_rate`
- 柴油机运行小时
- 柴油机启动次数
- SOC 统计

随后 `simulator.py` 再把这些结果转换成前端需要的结果字段。

### 5.17 成本拆分怎么做

`build_template_cost_breakdown()` 来自 `template_cost_engine.py`。

它会组合：

- 产品目录
- 系统配置
- 仿真结果
- 配套成本规则

生成几类 CAPEX 明细：

- 光伏
- 储能
- 柴油
- 电气附件
- 物流安装

同时生成 OPEX：

- 固定微电网运维
- 设备维修
- 保险
- 柴油维护

这一步的意义是：后端不是只给总价，而是保留了模板化可追溯的成本结构。

### 5.18 经济分析怎么做

`calculate.py` 最后调用 `generate_homer_economic_report()`。

它接收：

- 项目参数 `ProjectParameters`
- CAPEX
- 成本拆分
- 微电网仿真结果
- 柴油-only 仿真结果
- 电池寿命参数
- 柴油机购置/更换成本

输出：

- `summary`
- `tables["comparison"]`

最终由 `calculate.py` 整理成：

- `systemConfig`
- `capex`
- `simulation`
- `summary`
- `comparisonTable`

然后直接回给前端。

### 5.19 前端结果页如何消费这些数据

`ResultPage.tsx` 读取：

- `apiResult.systemConfig`
- `apiResult.capex`
- `apiResult.simulation`
- `apiResult.summary`
- `apiResult.comparisonTable`

然后分标签页展示：

- 方案展示
- 经济总览
- 仿真结果
- 系统配置

这里没有再向后端请求二次格式化数据，说明结果页是“后端给统一结构，前端直接渲染”。

---

## 6. DIY 流程：前后端交互与数据流

DIY 场景和已知负载的最大区别是：

- 不先走 `/api/optimize`
- 直接进入 `/api/calculate?simulate=true`

### 6.1 为什么 DIY 不走推荐

`useWizardCalculation.ts` 里逻辑非常明确：

- 只有 `scenario === 'known-load'` 且 `annualLoadKwh > 0` 时，才走 `/api/optimize`
- 否则直接走正式计算

这说明 DIY 被认为是用户已经基本确定设备意图，不需要先从候选方案里挑。

### 6.2 DIY 发起正式计算前前端会补算什么

DIY 在前端会补算几个关键量：

1. 从电池包型号查出单包 `capacityKwh`
2. 根据 `batteryPackCount` 算总储能
3. 根据 `inverterKw * inverterCount` 算总逆变功率
4. 由 `batteryKwh / inverterKw` 推算备电时长
5. 再把备电时长换成 `storageDays`

换句话说，DIY 虽然用户看上去是直接选设备，但前端仍然会把这些配置标准化成后端可理解的字段。

### 6.3 DIY 正式计算后和已知负载的结果链是否相同

相同。

DIY 一旦进入 `/api/calculate?simulate=true`，后续后端链路完全一致：

- `compute_sizes`
- `run_pypsa`
- `build_template_cost_breakdown`
- `generate_homer_economic_report`
- 返回统一结果结构

因此，前后端共享的是同一套最终结果模型。

---

## 7. 地图与 Pelias 功能的完整实现逻辑

这是用户特别关心的部分。

### 7.1 地图功能分成哪几层

地图能力实际上由三层组成：

1. 底图显示层
   - Leaflet + ArcGIS World Imagery
2. 前端交互层
   - 点选位置、拖动 Marker、量线、画多边形
3. 地理编码层
   - 前端请求本项目后端
   - 后端再请求 Pelias

### 7.2 纯地图显示不是 Pelias 实现的

这一点必须说清楚：

- 地图瓦片底图来自 ArcGIS：
  - `https://server.arcgisonline.com/.../World_Imagery/...`
- Pelias 不负责显示地图底图
- Pelias 只负责地址与经纬度转换

所以：

```text
Leaflet = 地图容器与交互
ArcGIS Tile = 地图影像底图
Pelias = 地址搜索 / 逆地理编码
```

三者职责完全不同。

### 7.3 `SiteAreaMap.tsx` 的定位模式

地图组件维护了 `mode`：

- `select`
- `line`
- `polygon`

含义分别是：

- `select`：设置项目位置
- `line`：测量两点直线距离
- `polygon`：绘制场地多边形并计算可安装布局

### 7.4 项目位置选择的数据流

#### 方式 A：在地图上点击

流程是：

1. Leaflet 触发 `map.on('click')`
2. 当前模式是 `select`
3. 调用 `handleSiteSelection(latlng)`
4. 先立即把坐标写入前端：
   - `latitude`
   - `longitude`
   - `locationName` 先用经纬度字符串兜底
5. 再异步请求逆地理编码：
   - `fetchReverseGeocode(lat, lon, countryCode)`
6. 如果成功：
   - 用解析出的地址替换 `locationName`
7. 如果失败：
   - 保留经纬度兜底文本，并显示错误提示

这里的设计很合理：

- 坐标更新不依赖地理编码成功
- 地理编码只是补充更友好的地址名称

#### 方式 B：拖动 Marker

流程和点击地图本质一样，只是触发源是：

- `marker.on('dragend')`

拖动结束后同样进入 `handleSiteSelection()`。

### 7.5 文本地址搜索的数据流

`StepLocation.tsx` 中点击搜索按钮后：

1. 读取输入框文本 `projectLocation`
2. 用正则判断是否含中文
3. 决定 `countryCode = cn / us`
4. 调用 `fetchGeocode(query, countryCode)`
5. 前端请求：
   - `/api/geocode?q=...&country_code=...`
6. 取返回结果第一项
7. 提取：
   - `lat`
   - `lon`
   - `formatted_address / display_name`
8. 调用 `syncResolvedCoords()`
9. 写回 `config`：
   - `latitude`
   - `longitude`
   - `locationName`
10. 再自动触发太阳辐照参数查询

所以文本搜索不是只为了显示地址，而是最终仍然要归结到经纬度。

### 7.6 浏览器当前位置的数据流

点击 “Use Current Location” 后：

1. 调用 `navigator.geolocation.getCurrentPosition`
2. 浏览器返回当前经纬度
3. 先依据坐标范围判断 CN / US
4. 调用 `fetchReverseGeocode()`
5. 若逆地理编码成功：
   - 得到可读地址名
6. 若失败：
   - 使用格式化经纬度当地点名
7. 坐标和地点名统一回写 `config`
8. 再调用太阳资源查询

### 7.7 `/api/geocode` 后端到底做了什么

后端实现文件：`backend/app/routers/geocode.py`

请求进来后主要步骤：

1. 根据 `country_code` 解析使用哪组 geocoder URL
2. 只允许 Pelias URL
3. 将前端参数转换成 Pelias 期望参数：
   - 前端的 `q`
   - 转为 Pelias 的 `text`
   - 增加 `lang`
   - 增加 `limit/size`
4. 用 `httpx.AsyncClient` 请求 Pelias `/v1/search`
5. 如果失败且是中国区域：
   - 尝试本地中国城市 fallback 列表
6. 如果成功：
   - 把 Pelias `FeatureCollection` 归一化成前端统一数组结构

#### 归一化结果结构

后端返回给前端的每一项大致是：

- `lat`
- `lon`
- `display_name`
- `formatted_address`
- `address`
- `raw`

所以前端完全不需要直接理解 Pelias 的 GeoJSON 细节。

### 7.8 `/api/reverse-geocode` 后端做了什么

逻辑与正向搜索类似，但会把参数改造成 Pelias 反查格式：

- `lat` -> `point.lat`
- `lon` -> `point.lon`
- `size=1`
- `lang=zh/en`

收到 Pelias 结果后：

- 取第一条 feature
- 提取 `properties`
- 组装统一的：
  - `display_name`
  - `formatted_address`
  - `address`
  - `raw`

前端因此只面向本项目定义好的格式，而不是直接消费第三方格式。

### 7.9 中国 fallback 是怎么实现的

`geocode.py` 里定义了一大组 `LOCAL_CN_PLACES`。

当以下情况发生时：

- Pelias 请求失败
- 或中国区域查询无结果

后端会执行 `_local_cn_geocode(query)`：

1. 对中文地名做规范化去后缀
2. 与内置城市、别名进行模糊匹配
3. 返回最多 5 个候选结果

这意味着项目对中国常见城市搜索做了一个兜底策略：

- Pelias 掉了，不至于地图功能全废
- 但这个 fallback 只适合简单城市级搜索，不是完整地理编码系统

### 7.10 Pelias 在这里到底扮演什么角色

Pelias 只负责两件事：

1. 文本地址 -> 经纬度
2. 经纬度 -> 可读地址

它不负责：

- 地图底图显示
- 场地面积计算
- 支架排布
- 日照查询
- 微电网选型或仿真

所以 Pelias 是“地图交互里的地址解析基础设施”，不是整个地图功能的全部。

### 7.11 地图多边形量测和支架排布是怎么实现的

这部分完全是前端本地计算，和 Pelias 无关。

`SiteAreaMap.tsx` 在用户绘制多边形后会：

1. 把经纬度点转换到局部平面坐标
2. 计算多边形面积、周长、质心
3. 枚举候选旋转角度
4. 对每个角度尝试支架网格排布
5. 支架尺寸使用固定参数：
   - 长 `28m`
   - 宽 `5.6m`
   - 相邻间距 `3.048m`（10 英尺）
6. 检查每个矩形支架是否完整落在多边形内部
7. 选出可放置数量最多的布局
8. 在地图上把每套支架矩形渲染出来
9. 返回 `installableSets`

所以“场地可安装多少套支架”的功能不是后端算的，也不是 Pelias 算的，而是前端 Leaflet 组件本地几何算法算的。

### 7.12 日照参数和地图是什么关系

坐标确定后，前端还会调用：

```text
/api/solar-hours?lat=...&lon=...
```

这个接口在 `backend/app/routers/products.py` 中。

后端优先调用 NASA POWER 日度辐照数据，计算：

- `peak_sun_hours_per_day`
- `annual_kwh_per_m2`
- `annual_eff_hours`
- `climate_zone`

若 NASA POWER 不可用，则退回基于纬度的经验估算。

所以地图坐标还会影响：

- 日照参数展示
- 后续推荐与仿真使用的太阳资源条件

---

## 8. 报告导出链路

### 8.1 前端怎么触发导出

结果页 `ResultPage.tsx` 点击“下载报告”后，会弹出 `DownloadReportModal.tsx`。

用户填写联系人信息后，前端发起：

```text
POST /api/send-report
```

请求体包含两部分：

1. `contact`
2. 当前页面已有的结果数据：
   - `systemConfig`
   - `capex`
   - `simulation`
   - `summary`
   - `comparisonTable`

注意这里不是让后端重新算一遍，而是把当前结果页已有的数据作为报告源数据提交。

### 8.2 后端 `/api/send-report` 做了什么

路由文件：`backend/app/routers/report.py`

处理顺序：

1. 生成报告文件名
2. 调用 `build_solution_report(req)` 生成 Excel 二进制
3. 转成 Base64 返回
4. 如果 SMTP 已配置，同时发邮件

返回结构包括：

- `fileBase64`
- `fileName`
- `emailSent`
- `smtpConfigured`

### 8.3 前端如何真正下载文件

前端收到 `fileBase64` 后：

1. `atob()` 解码为二进制
2. 构造 `Blob`
3. `URL.createObjectURL(blob)`
4. 动态创建 `<a>` 标签并触发点击
5. 浏览器开始下载 Excel

也就是说，文件不是后端返回一个下载 URL，而是直接把文件内容回传给浏览器。

---

## 9. 核心数据流总图

### 9.1 前端主业务流

```text
用户输入
  -> 前端 config 状态持续更新
  -> 某些输入触发地图/地理编码/太阳资源查询
  -> 进入推荐或正式计算
  -> 后端返回统一结果结构
  -> 前端结果页渲染
  -> 可选导出报告
```

### 9.2 已知负载流

```text
表单输入 + 地图选址 + 场地测量
  -> config
  -> /api/optimize
  -> 推荐方案列表
  -> 用户选中方案
  -> 更新 config
  -> /api/calculate?simulate=true
  -> compute_sizes
  -> run_pypsa
  -> cost breakdown
  -> economic report
  -> ResultPage
```

### 9.3 DIY 流

```text
DIY 设备参数输入
  -> 前端补算 storageDays / inverter / battery
  -> /api/calculate?simulate=true
  -> 后端正式计算
  -> ResultPage
```

### 9.4 地图与 Pelias 流

```text
用户输入地址 / 点地图 / 拖 marker / 浏览器定位
  -> 前端请求 /api/geocode 或 /api/reverse-geocode
  -> FastAPI geocode proxy
  -> Pelias /v1/search 或 /v1/reverse
  -> 后端归一化结构
  -> 前端写入 config.latitude / longitude / locationName
  -> 再请求 /api/solar-hours
  -> 返回太阳资源参数
```

### 9.5 报告流

```text
结果页已有数据
  + 联系人信息
  -> /api/send-report
  -> 后端生成 Excel
  -> base64 返回
  -> 浏览器下载
  -> 可选 SMTP 发邮件
```

---

## 10. 这个项目实现上的关键特点

### 10.1 优点

1. 前后端职责边界清楚

- 前端负责交互、状态、地图几何计算、结果展示
- 后端负责规格推导、仿真、经济分析、报表生成

2. 推荐链路和正式计算链路分离

- 推荐快
- 正式结果精度更高

3. Pelias 被放在后端代理之后

- 前端不直接依赖第三方返回格式
- 可以统一做错误处理和 fallback

4. 没有数据库，链路相对干净

- 调试和追踪数据流比较直接

### 10.2 需要注意的实现现实

1. 推荐链路与正式链路模型不完全一致

- `/api/optimize` 主要依赖 `quick_estimate`
- `/api/calculate?simulate=true` 才跑完整 PyPSA

所以推荐方案排序和最终结果可能存在偏差，这是架构层面的设计选择。

2. 地图中的场地排布完全在前端

- 算法可视化效果很好
- 但属于浏览器本地几何规则，不是后端统一约束

3. Pelias 是外部基础设施

- 一旦 Pelias 没启动，地图地址搜索和逆地理编码会受影响
- 但点位和部分中国城市搜索仍有兜底

4. 结果页和报表导出复用了同一份结果数据

- 一致性较好
- 也意味着如果前端结果状态不完整，导出也会受影响

---

## 11. 一句话总结整个项目怎么实现

这个项目的本质是：

前端用一个统一的 `config` 状态对象收集用户的站点、场地、负荷、设备和经济参数；地图层负责选址和场地排布，地址解析通过 FastAPI 代理转发给 Pelias；已知负载场景先用快速优化给出推荐方案，用户选中后再进入正式计算；后端基于 `products.yaml`、规格推导、NASA/PVLIB/PyPSA 仿真和经济模型生成统一结果 JSON，前端再把这份结果渲染成方案页，并可进一步导出为 Excel 报告。

